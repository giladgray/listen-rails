<h1>Listing lists</h1>

<table>
  <tr>
    <!--<th colspan=""n="2">Name</th>
    <th>Tagline</th>
    <th>Description</th>
    <th>Type</th>
    <th>User</th>
    <th>Rating</th> -->
    <th></th>
  </tr>

<% @lists.each do |list| %>
  <tr>
    <td colspan='2'><h3><%= link_to list.name, list %>
        <small><%= list.tagline %></small></h3>
        <%= list.description %></td>
    <!--  <td><%= list.list_type %></td>
    <td><%= list.user_id %></td>
    <td><%= list.rating %></td> -->
    <td><div class='btn-group'>
        <%= link_to edit_list_path(list), class: 'btn' do %><i class='icon-share'></i><% end %>
        <%= link_to list, confirm: 'Are you sure?', method: :delete, class: 'btn' do %><i class='icon-trash'></i><% end %>
    </div></tr>
<% end %>
</table>

<br />

<%= link_to 'New List', new_list_path, class: 'btn btn-info' %>
