<p id="notice"><%= notice %></p>

<div class="page-header">
	<h1><%= link_to @list.name, @list %>
			<small><%= @list.tagline %></small></h1>
	<%= @list.description %>
</div>

<ul class="things">
<% @list.things.each do |thing| %>
	<li><%= link_to thing.name, thing %></li>
<% end %>
	<li><a id="newThing" class="btn btn-primary btn-large" href="/lists/<%= @list.id %>/add">&plus;</a></li>
</ul>

<%= link_to 'Edit', edit_list_path(@list) %> |
<%= link_to 'Back', lists_path %>
